package ObjectOriented;

public class ObjectOriented03Difference {
    /*
     * 局部变量与成员变量的差异：
     * 1.前者在方法内部定义，后者在方法外部，直接在类中定义
     * 2.前者只适用于方法中，后者可以在整个类中通用
     * 3.前者没有默认值，只有赋值后才能使用，后者采取与数组相同的默认值
     * 4.前者位于栈内存，后者位于堆内存
     * 5.前者随方法压栈产生方法退栈消失，后者随对象创建而产生对象被JVM回收而消失
     * */
}
